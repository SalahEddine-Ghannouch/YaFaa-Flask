Oussama = {
	1 : retrive the lineups for all the fixtures.
	2 : install hbase to transfer the needed data from hadoop file system to the HBase. 
	2 : store all the data in our hadoop system instead of the tested hadoop.
}

aymane = {
    1 : add the clubs elos to the code
    2 : try to plot the elos
    3 : fix the side bar
}

salah = {

    1 : get fixtures stats in pages,
    2 : choose stats for index page,
    ==> run this to generate reque.txt : pipreqs .

}

imad = {
    1 - push the data + code
    2 - plot the fixtures using plotly
}

both = {
    add your image to static/svg/team
}